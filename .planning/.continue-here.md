---
context: default
phase: post-audit-bugfix
task: 3
total_tasks: 6
status: in_progress
last_updated: 2026-04-26T01:00:58.739Z
---

# BLOCKING CONSTRAINTS — Leia Antes de Qualquer Coisa

> Estas não são sugestões. Cada constraint abaixo foi descoberta durante a sessão.

- [ ] CONSTRAINT: humanTakeover-bypass — O check `isAiBlocked` em `service.ts:2988` usa `!isAdminOrInstanceSender` para não silenciar o admin, mas `queueConversationTurn` na linha 3641 é chamado SEM guard de `isAdminOrInstanceSender`. Mensagens fromMe do atendente humano (linked device) passam pelo pipeline do chatbot. NÃO presuma que o check em 2988 é suficiente — não é.

- [ ] CONSTRAINT: legacy-block-ainda-presente — O bloco `if (finalInputText && isVerifiedAprendizadoContinuoAdminSender) { const handled = await this.adminCommandService.handleCommand(...) }` em `service.ts:~3479` NÃO foi removido no Phase 7 como planejado. Causa double-execution de comandos de texto livre do admin. Precisa ser deletado COM trace de execução antes.

**Não prossiga até confirmar que entendeu os dois constraints acima.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| isAiBlocked bypass para admin | `isAiBlocked && !isAdminOrInstanceSender` em 2988 não bloqueia mensagens admin mesmo com humanTakeover=true | blocking | Adicionar `if (!isAdminOrInstanceSender)` antes de `queueConversationTurn` em linha 3641 |
| Legacy block dupla execução | adminCommandService.handleCommand() em 3479 executa EM PARALELO com AdminCommandHandler | advisory | Deletar bloco após trace de execução |

<current_state>
Sessão de milestone audit + investigação de bug encerrada por esgotamento de contexto.

**Concluído:**
- Milestone audit v1.0 executado → `.planning/v1.0-MILESTONE-AUDIT.md` criado (status: gaps_found, não commitado ainda)
- Root cause do bug humanTakeover identificado: `service.ts:3641` — `queueConversationTurn` sem guard `isAdminOrInstanceSender`

**Pendente (4 tarefas):**
1. Remover bloco legado `service.ts:~3479`
2. Atualizar REQUIREMENTS.md (IA-02 e DOC-03)
3. Corrigir bug humanTakeover em `service.ts:3641`
4. Commitar audit file + rodar `/gsd-complete-milestone v1.0`
</current_state>

<completed_work>

- **Audit v1.0 completo:** `.planning/v1.0-MILESTONE-AUDIT.md` criado com análise 3-source de 47 requisitos, status por fase, blockers, dívida técnica, e recomendações.
- **Bug humanTakeover diagnosticado:** Lidas linhas 2015-3641 de service.ts. Root cause: queueConversationTurn em linha 3641 não tem guard isAdminOrInstanceSender. Mensagens do atendente (fromMe: true de linked device) passam pelo pipeline.
- **Preferência de idioma salva:** Memória criada para responder em pt-BR.
</completed_work>

<remaining_work>

**Tarefa 3 — Remover bloco legado (service.ts:~3479):**
- Localizar: `if (finalInputText && isVerifiedAprendizadoContinuoAdminSender) {` em torno da linha 3479
- Verificar: fazer grep para confirmar que nenhum fluxo único depende desse bloco
- Deletar: o bloco inteiro até o `}` de fechamento
- Testar: grep deve retornar zero matches para `isVerifiedAprendizadoContinuoAdminSender.*handleCommand`

**Tarefa 4 — Atualizar REQUIREMENTS.md:**
- IA-02: linha com `DUVIDA_GENERICA` → mudar para `PERGUNTA, CONTINUACAO, OUTRO`
- DOC-03: linha "Arquivo referenciado por URL — não buffer em memória para arquivos grandes" → mudar para "Arquivo enviado via base64 com gate de 5 MB antes de readFile() — Baileys não suporta file:// URLs locais"

**Tarefa 5 — Corrigir bug humanTakeover (service.ts:~3641):**
- Localizar: `this.queueConversationTurn(session, finalInputText, {` em linha ~3641
- Fix: envolver em `if (!isAdminOrInstanceSender) {` ... `}`
- Alternativa: verificar se queueConversationTurn (linha 4257) já tem guard interno — se não, adicionar o guard no call site (linha 3641)
- Testar: verificar que com humanTakeover ativo, mensagem do atendente (fromMe) não gera resposta do chatbot

**Tarefa 6 — Finalizar milestone:**
- `git add .planning/v1.0-MILESTONE-AUDIT.md` e commitar
- Rodar `/gsd-complete-milestone v1.0`
</remaining_work>

<decisions_made>

- **Aceitar lacunas de VERIFICATION.md (fases 2,3,4,6,8) como dívida técnica** — Fases têm SUMMARY.md com evidência de implementação e testes. Criar VERIFICATION.md retroativo para todas seria excessivo.
- **DOC-03: aceitar base64 buffer** — Baileys não suporta file:// em resolveMediaBuffer. Base64 + gate 5 MB é a única opção viável.
- **IA-02: atualizar REQUIREMENTS.md** — Problema de nomenclatura, não de funcionalidade. ROADMAP já usa PERGUNTA/CONTINUACAO/OUTRO.
- **humanTakeover fix: guard no call site (linha 3641)** — Mais cirúrgico, não afeta outros caminhos de admin que devem continuar funcionando.
</decisions_made>

<blockers>
- Nenhum bloqueio hard. O trace de execução do bloco legado é recomendado mas não é bloqueante para o fix.
</blockers>

## Required Reading (em ordem)
1. `.planning/v1.0-MILESTONE-AUDIT.md` — contexto completo do audit e o que precisa ser corrigido
2. `apps/api/src/modules/instances/service.ts` linhas 2982-3660 — fluxo completo da mensagem, humanTakeover check, e queueConversationTurn
3. `apps/api/src/modules/instances/service.ts` linhas 3470-3535 — bloco legado a remover

## Infrastructure State
- Nenhum servidor rodando
- `.planning/v1.0-MILESTONE-AUDIT.md` criado mas NÃO commitado
- Branch: master

<context>
Sessão de /gsd-audit-milestone seguida de investigação de bug humanTakeover. O audit identificou 1 bloqueio hard (legacy block em 3479), 2 decisões (IA-02 nomenclatura, DOC-03 base64), e 5 fases sem VERIFICATION.md.

O usuário relatou bug: "a IA se auto responde quando o atendente humano fala". Investigation revelou que queueConversationTurn (linha 3641) não tem guard isAdminOrInstanceSender. Mensagens fromMe do atendente passam pelo pipeline do chatbot porque o check isAiBlocked em 2988 não bloqueia mensagens de admin.

O usuário prefere comunicação em português (pt-BR).
</context>

<next_action>
1. Ler `service.ts` linhas 3470-3535 para confirmar localização exata do bloco legado
2. Fazer grep: `grep -n "isVerifiedAprendizadoContinuoAdminSender.*handleCommand\|handleCommand.*isVerified" service.ts`
3. Deletar o bloco legado
4. Atualizar REQUIREMENTS.md (IA-02 e DOC-03)
5. Ler service.ts linhas 3635-3660 e 4257-4300 para confirmar fix correto do humanTakeover
6. Aplicar fix em service.ts:3641
7. Commitar tudo + rodar /gsd-complete-milestone v1.0
</next_action>
