---
phase: 02-crm-identity-data-integrity
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/modules/crm/__tests__/lid-normalization.test.ts
  - apps/api/src/modules/crm/__tests__/format-phone.test.ts
  - apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts
  - apps/api/src/lib/__tests__/run-migrations.test.ts
autonomous: true
requirements: [CRM-01, CRM-02, CRM-03, CRM-04, CRM-05, CRM-06]

must_haves:
  truths:
    - "All four test stub files exist with failing placeholder tests"
    - "pnpm vitest run exits non-zero (tests fail as expected in Wave 0)"
    - "Test file names match VALIDATION.md task IDs exactly"
  artifacts:
    - path: "apps/api/src/modules/crm/__tests__/lid-normalization.test.ts"
      provides: "Failing stubs for LID→phoneNumber and rawJid fallback behaviors"
    - path: "apps/api/src/modules/crm/__tests__/format-phone.test.ts"
      provides: "Failing stubs for all formatPhone() input classes from UI-SPEC"
    - path: "apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts"
      provides: "Failing stub for N+1 batch query replacement"
    - path: "apps/api/src/lib/__tests__/run-migrations.test.ts"
      provides: "Failing stubs for runMigrations error isolation and startup summary"
  key_links:
    - from: "format-phone.test.ts"
      to: "apps/api/src/lib/format-phone.ts"
      via: "import { formatPhone } from '../../../lib/format-phone'"
      pattern: "from.*lib/format-phone"
    - from: "run-migrations.test.ts"
      to: "apps/api/src/lib/run-migrations.ts"
      via: "import { runMigrations } from '../run-migrations'"
      pattern: "from.*run-migrations"
---

<objective>
Create Wave 0 test scaffold stubs for all automated tests in Phase 2. Every test file must exist and fail (RED) before implementation begins. This ensures the Nyquist rule is satisfied — no implementation task runs without a pre-existing test file to turn green.

Purpose: Establish the RED baseline so subsequent waves can measure GREEN completion. Without these stubs, executors cannot verify their work via `pnpm vitest run`.
Output: Four test stub files, all failing with `expect(true).toBe(false)` or `todo()` markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-crm-identity-data-integrity/02-CONTEXT.md
@.planning/phases/02-crm-identity-data-integrity/02-VALIDATION.md
@.planning/phases/02-crm-identity-data-integrity/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LID normalization and formatPhone test stubs</name>
  <files>
    apps/api/src/modules/crm/__tests__/lid-normalization.test.ts
    apps/api/src/modules/crm/__tests__/format-phone.test.ts
  </files>

  <read_first>
    - apps/api/src/modules/crm/ (list directory — understand existing test layout)
    - .planning/phases/02-crm-identity-data-integrity/02-VALIDATION.md (task IDs and test names)
    - .planning/phases/02-crm-identity-data-integrity/02-UI-SPEC.md (formatPhone input/output contract)
  </read_first>

  <action>
Create `apps/api/src/modules/crm/__tests__/` directory if absent. Create two test stub files:

**File 1: `lid-normalization.test.ts`**

```typescript
import { describe, it, expect, vi } from "vitest";

// Stubs — all tests start RED. Implementation in Plan 2.1 turns them GREEN.

describe("LID normalization", () => {
  it("stores null in phoneNumber when remoteJid ends with @lid", () => {
    // TODO: import and call the upsert helper after Plan 2.1
    expect(true).toBe(false); // RED stub
  });

  it("stores raw @lid string in rawJid when phoneNumber is null", () => {
    // TODO: verify rawJid = event.remoteJid after Plan 2.1 schema change
    expect(true).toBe(false); // RED stub
  });

  it("does not write @lid digits into phoneNumber column", () => {
    // TODO: confirm storedContactPhoneNumber is never e.g. "19383773"
    expect(true).toBe(false); // RED stub
  });
});

describe("rawJid fallback", () => {
  it("upserts by instanceId_rawJid when remoteJid is @lid", () => {
    expect(true).toBe(false); // RED stub
  });

  it("leaves phoneNumber null when LID resolution fails", () => {
    expect(true).toBe(false); // RED stub
  });
});

describe("LID reconciliation", () => {
  it("enqueues BullMQ job lid-reconcile:{instanceId} on connection.update:open", () => {
    expect(true).toBe(false); // RED stub
  });

  it("does not enqueue duplicate jobs for same instanceId", () => {
    expect(true).toBe(false); // RED stub
  });
});
```

**File 2: `format-phone.test.ts`**

```typescript
import { describe, it, expect } from "vitest";
// TODO: uncomment after Plan 2.2 creates the module
// import { formatPhone } from "../../../lib/format-phone";

// All test cases derived from 02-UI-SPEC.md formatPhone() Output Contract

describe("formatPhone", () => {
  it("formats BR 11-digit mobile: 5511987654321 → +55 11 98765-4321", () => {
    expect(true).toBe(false); // RED stub
  });

  it("formats BR 11-digit with leading +: +5511987654321 → +55 11 98765-4321", () => {
    expect(true).toBe(false); // RED stub
  });

  it("formats BR 10-digit landline: 551198765432 → +55 11 9876-5432", () => {
    expect(true).toBe(false); // RED stub
  });

  it("returns E.164 as-is for non-BR international: +14155550199 → +14155550199", () => {
    expect(true).toBe(false); // RED stub
  });

  it("returns 'Aguardando número' for null input", () => {
    expect(true).toBe(false); // RED stub
  });

  it("strips @c.us suffix before formatting: 5511987654321@c.us → +55 11 98765-4321", () => {
    expect(true).toBe(false); // RED stub
  });

  it("returns 'Contato desconhecido' for non-numeric garbage after stripping", () => {
    expect(true).toBe(false); // RED stub
  });

  it("never returns a string containing @lid", () => {
    expect(true).toBe(false); // RED stub
  });
});
```
  </action>

  <verify>
    <automated>pnpm vitest run --reporter=verbose apps/api/src/modules/crm/__tests__/lid-normalization.test.ts apps/api/src/modules/crm/__tests__/format-phone.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/src/modules/crm/__tests__/lid-normalization.test.ts` exists: `ls apps/api/src/modules/crm/__tests__/lid-normalization.test.ts` exits 0
    - `apps/api/src/modules/crm/__tests__/format-phone.test.ts` exists: `ls apps/api/src/modules/crm/__tests__/format-phone.test.ts` exits 0
    - Both files contain `expect(true).toBe(false)`: `grep -c "expect(true).toBe(false)" apps/api/src/modules/crm/__tests__/lid-normalization.test.ts` returns >= 6
    - vitest output includes "FAIL" for both files (RED is expected at this stage)
  </acceptance_criteria>

  <done>Both test files exist, contain failing RED stubs matching the behavior names in VALIDATION.md, and vitest reports them as failed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CRM contacts batch and run-migrations test stubs</name>
  <files>
    apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts
    apps/api/src/lib/__tests__/run-migrations.test.ts
  </files>

  <read_first>
    - apps/api/src/lib/ (list directory — understand existing structure)
    - .planning/phases/02-crm-identity-data-integrity/02-VALIDATION.md (task IDs 02-03-01, 02-04-01)
    - .planning/phases/02-crm-identity-data-integrity/02-RESEARCH.md (Pattern 3 runMigrations, Pattern 6 N+1 fix)
  </read_first>

  <action>
Create `apps/api/src/lib/__tests__/` directory if absent.

**File 1: `crm-contacts-batch.test.ts`**

```typescript
import { describe, it, expect, vi } from "vitest";

// Stub — RED. Implementation in Plan 2.3 turns GREEN.

describe("CRM contacts batch", () => {
  it("replaces N+1 clientMemory.findFirst loop with single findMany query", () => {
    // TODO: import listContacts handler after Plan 2.3 refactor
    // Verify: prisma.clientMemory.findFirst is never called; findMany called once
    expect(true).toBe(false); // RED stub
  });

  it("returns correct memory per contact matched by last-8-digit phone suffix", () => {
    expect(true).toBe(false); // RED stub
  });

  it("handles empty contact list without querying clientMemory", () => {
    expect(true).toBe(false); // RED stub
  });
});
```

**File 2: `run-migrations.test.ts`**

```typescript
import { describe, it, expect, vi } from "vitest";

// Stub — RED. Implementation in Plan 2.4 turns GREEN.

describe("runMigrations", () => {
  it("returns 'success' when all migrations applied without error", () => {
    expect(true).toBe(false); // RED stub
  });

  it("returns 'skipped' when all migrations already applied", () => {
    expect(true).toBe(false); // RED stub
  });

  it("catches per-tenant error and returns 'failed' without throwing", () => {
    // Must NOT propagate — API startup continues even if one tenant fails
    expect(true).toBe(false); // RED stub
  });

  it("logs structured { tenantId, migration, error } at error level on failure", () => {
    expect(true).toBe(false); // RED stub
  });

  it("applies only unapplied migrations (version tracking)", () => {
    expect(true).toBe(false); // RED stub
  });

  it("creates schema_migrations table if absent before reading applied versions", () => {
    expect(true).toBe(false); // RED stub
  });
});
```
  </action>

  <verify>
    <automated>pnpm vitest run --reporter=verbose apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts apps/api/src/lib/__tests__/run-migrations.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts` exists: `ls apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts` exits 0
    - `apps/api/src/lib/__tests__/run-migrations.test.ts` exists: `ls apps/api/src/lib/__tests__/run-migrations.test.ts` exits 0
    - run-migrations.test.ts contains 6 it() blocks: `grep -c "^  it(" apps/api/src/lib/__tests__/run-migrations.test.ts` returns 6
    - vitest reports both files as FAIL (RED is expected)
  </acceptance_criteria>

  <done>All four Wave 0 test stubs exist. pnpm vitest run reports failures for all four files. The VALIDATION.md task IDs (02-01-01, 02-01-02, 02-01-03, 02-02-01, 02-03-01, 02-04-01) are represented by named it() blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production modules | Test imports must not execute real DB or queue operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Information Disclosure | test stubs | accept | Test files contain no secrets or real credentials — stubs only |
| T-02-00-02| Tampering | test file import paths | mitigate | Import paths must use `vi.mock()` or TODO markers; no live DB calls in stubs |
</threat_model>

<verification>
```bash
# All four stub files exist
ls apps/api/src/modules/crm/__tests__/lid-normalization.test.ts
ls apps/api/src/modules/crm/__tests__/format-phone.test.ts
ls apps/api/src/modules/crm/__tests__/crm-contacts-batch.test.ts
ls apps/api/src/lib/__tests__/run-migrations.test.ts

# All fail (RED baseline confirmed)
pnpm vitest run --reporter=verbose 2>&1 | grep -E "FAIL|PASS"
```
</verification>

<success_criteria>
- All four test stub files created and committed
- pnpm vitest run reports all four files as FAIL (expected — RED phase)
- No production code modified in Wave 0
- Test descriptions match the behavior names in VALIDATION.md task ID map
</success_criteria>

<output>
After completion, create `.planning/phases/02-crm-identity-data-integrity/02-00-SUMMARY.md`
</output>
