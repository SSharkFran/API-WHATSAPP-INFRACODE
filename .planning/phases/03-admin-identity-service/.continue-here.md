---
context: phase
phase: 03-admin-identity-service
task: 1
total_tasks: 2
status: not_started
last_updated: 2026-04-14T18:24:21.930Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified for 03-04 specifically. See advisory anti-patterns below._

**Proceed when ready.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Discriminated union cast workaround | In 03-02, agent added `as WorkerEvent` cast instead of adding `AdminJidResolvedWorkerEvent` to the WorkerEvent union type in packages/types — caused TS2367 build failure caught post-execution | advisory | Always update union types in packages/types when adding new event variants; never use type casts to silence TS errors |
| ForbiddenError swallowing in API wrappers | In 03-03, `getAdminTenants()`, `getAdminBilling()`, `getAdminPlans()` re-threw ForbiddenError as generic Error, breaking instanceof check in page catch blocks | advisory | When catching and re-throwing in wrapper functions, always pass-through specific error subclasses with `if (err instanceof SpecificError) throw err` before the generic re-throw |

<current_state>
Phase 03 (admin-identity-service) is at Wave 3. Plans 03-01, 03-02, and 03-03 are all complete and committed. Plan 03-04 has NOT been started — the session ended before execution began. The plan file exists at `.planning/phases/03-admin-identity-service/03-04-PLAN.md` and is ready to execute.
</current_state>

<completed_work>

Completed Plans:
- Plan 03-01: AdminIdentityService extraction — Done (commit 130a363)
- Plan 03-02: Redis JID cache for @lid resolution — Done (commit b2b15be, plus TS fix 84e794f)
- Plan 03-03: Platform route audit + panel error states — Done (commit 2984d36, plus ForbiddenError fix 962ce56)
</completed_work>

<remaining_work>

- Plan 03-04: Housekeeping fixes — NOT STARTED
  - Task 1: Replace all console.* with Pino structured logger in `service.ts` and `chatbot/service.ts`
  - Task 2: Fix scheduler lifecycle (startSchedulers after listen, stopSchedulers in onClose), delete ~80-line dead code block, fix UTF-8 corruption on ~line 4887, add worker exit DB status update
</remaining_work>

<decisions_made>

- ForbiddenError must be re-thrown explicitly by all getAdmin* wrappers in `apps/panel/lib/api.ts` before any generic catch re-throw — otherwise page-level `instanceof ForbiddenError` breaks silently
- AdminJidResolvedWorkerEvent must be added to the WorkerEvent discriminated union in `packages/types` (not just cast) — TypeScript strict mode catches the mismatch at build time
- Pino logger uses `createLogger(config).child({ component: "..." })` pattern — see `apps/api/src/lib/logger.ts` and existing usage in `apps/api/src/app.ts` lines 78–85
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-admin-identity-service/03-04-PLAN.md` — the full plan for the housekeeping tasks; has exact file targets, interfaces, and acceptance criteria
2. `.planning/phases/03-admin-identity-service/03-RESEARCH.md` — original research context for the phase

## Critical Anti-Patterns (do NOT repeat these)
- **CAST WORKAROUND**: Never silence TS union errors with `as SomeType` → always extend the union in `packages/types`
- **SWALLOWED SUBCLASS ERRORS**: Wrapper catch blocks must `if (err instanceof SpecificError) throw err` before generic re-throw

## Infrastructure State
- No running servers required for 03-04 (all changes are code-only: logging, lifecycle, dead code, UTF-8)
- TypeScript check: `cd apps/api && pnpm tsc --noEmit`
- Test suite: `cd apps/api && pnpm test`

<context>
Phase 03 is nearly complete. Plans 01–03 are done and all verified. Plan 04 is pure housekeeping — no new features, no new architecture. The plan is self-contained with exact line references, interfaces, and acceptance criteria. The two tasks are sequential (Task 1: console→Pino; Task 2: scheduler/dead-code/UTF-8/worker-exit). After 03-04 completes, the phase is finished.
</context>

<next_action>
Start with: `/gsd-execute-phase` — the only remaining work is plan 03-04 at `.planning/phases/03-admin-identity-service/03-04-PLAN.md`. Both tasks are `type="auto"` and can be executed without discussion.
</next_action>
