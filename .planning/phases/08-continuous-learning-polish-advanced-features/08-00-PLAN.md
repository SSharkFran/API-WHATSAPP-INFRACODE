---
phase: 08-continuous-learning-polish-advanced-features
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/modules/instances/__tests__/aprendizado-continuo.interface.test.ts
  - apps/api/src/modules/chatbot/__tests__/escalation-confirmation-gate.test.ts
  - apps/api/src/modules/chatbot/__tests__/knowledge-audit.test.ts
  - apps/api/src/modules/instances/__tests__/follow-up.service.test.ts
  - apps/api/src/modules/instances/__tests__/session-metrics-collector.test.ts
autonomous: true
requirements:
  - APR-01
  - APR-02
  - APR-04
  - APR-05
  - URG-01
  - FOL-01
  - FOL-02

must_haves:
  truths:
    - "All Wave 0 test files exist and fail with a clear TODO message — not missing-file errors"
    - "Running `npm --prefix apps/api test -- --passWithNoTests` exits 0 (stub tests skip gracefully)"
    - "Each stub test file imports the subject module path that will exist after its plan runs"
  artifacts:
    - path: "apps/api/src/modules/instances/__tests__/aprendizado-continuo.interface.test.ts"
      provides: "Failing stubs for APR-01 Null Object behavior"
    - path: "apps/api/src/modules/chatbot/__tests__/escalation-confirmation-gate.test.ts"
      provides: "Failing stubs for APR-02, APR-04 confirmation gate"
    - path: "apps/api/src/modules/chatbot/__tests__/knowledge-audit.test.ts"
      provides: "Failing stubs for APR-05 confirmedAt/confirmedByJid"
    - path: "apps/api/src/modules/instances/__tests__/follow-up.service.test.ts"
      provides: "Failing stubs for FOL-01, FOL-02"
    - path: "apps/api/src/modules/instances/__tests__/session-metrics-collector.test.ts"
      provides: "Extended with stub for URG-01 urgency DB persistence"
  key_links:
    - from: "aprendizado-continuo.interface.test.ts"
      to: "apps/api/src/modules/instances/aprendizado-continuo.interface.ts"
      via: "import path"
      pattern: "aprendizado-continuo.interface"
    - from: "follow-up.service.test.ts"
      to: "apps/api/src/modules/instances/follow-up.service.ts"
      via: "import path"
      pattern: "follow-up.service"
---

<objective>
Create Wave 0 test scaffolds for all Phase 8 behaviors. These are failing stubs — they prove the test infrastructure is wired correctly and fail for the right reason (missing implementation), not for import errors or test runner config issues.

Purpose: Nyquist compliance — every automated verify command in plans 01–04 must have a pre-existing test file it can run against.
Output: 5 test files with failing stubs. All files import from paths that will exist after their respective implementation plans run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-continuous-learning-polish-advanced-features/08-RESEARCH.md
@.planning/phases/08-continuous-learning-polish-advanced-features/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Null Object interface test stub (APR-01)</name>
  <files>apps/api/src/modules/instances/__tests__/aprendizado-continuo.interface.test.ts</files>
  <read_first>
    - apps/api/src/modules/chatbot/module-runtime.ts (AprendizadoContinuoModuleConfig shape — lines 114–129)
    - apps/api/src/modules/instances/__tests__/admin-identity.service.test.ts (existing test style for this directory)
    - apps/api/vitest.config.ts (test runner config)
  </read_first>
  <behavior>
    - Test 1: `DisabledAprendizadoContinuoModule.isEnabled()` returns `false`
    - Test 2: `DisabledAprendizadoContinuoModule.getAdminPhones()` returns `[]`
    - Test 3: `DisabledAprendizadoContinuoModule.getAdminJids()` returns `[]`
    - Test 4: `DisabledAprendizadoContinuoModule.processLearningReply()` resolves to `null`
    - Test 5: `DisabledAprendizadoContinuoModule.shouldSendDailySummary()` returns `false`
    - Test 6: `DisabledAprendizadoContinuoModule.buildDailySummary()` resolves to `""`
    - Test 7: `ActiveAprendizadoContinuoModule.isEnabled()` returns `true` when constructed with `{ isEnabled: true, verificationStatus: "VERIFIED", ... }`
  </behavior>
  <action>
  Create the test file as a stub that imports from the path that will exist after Plan 01:

  ```typescript
  import { describe, it, expect } from 'vitest';
  // These imports will fail until Plan 01 creates the files — that is intentional.
  // Wave 0: stubs exist so the verify command in Plan 01 has a target.
  import {
    DisabledAprendizadoContinuoModule,
    ActiveAprendizadoContinuoModule,
  } from '../aprendizado-continuo.disabled.js';

  describe('DisabledAprendizadoContinuoModule — APR-01 Null Object', () => {
    const mod = new DisabledAprendizadoContinuoModule();

    it('isEnabled() returns false', () => {
      expect(mod.isEnabled()).toBe(false);
    });
    it('getAdminPhones() returns []', () => {
      expect(mod.getAdminPhones()).toEqual([]);
    });
    it('getAdminJids() returns []', () => {
      expect(mod.getAdminJids()).toEqual([]);
    });
    it('processLearningReply() resolves to null', async () => {
      await expect(mod.processLearningReply('t1', 'i1', 'any')).resolves.toBeNull();
    });
    it('shouldSendDailySummary() returns false', () => {
      expect(mod.shouldSendDailySummary('t1', 'i1')).toBe(false);
    });
    it('buildDailySummary() resolves to empty string', async () => {
      await expect(mod.buildDailySummary('t1', 'i1')).resolves.toBe('');
    });
  });

  describe('ActiveAprendizadoContinuoModule — APR-01 active path', () => {
    it('isEnabled() returns true', () => {
      const mod = new ActiveAprendizadoContinuoModule({
        isEnabled: true,
        verificationStatus: 'VERIFIED',
        configuredAdminPhone: '+5511999990000',
        verifiedPhone: '+5511999990000',
        verifiedPhones: [],
        verifiedRemoteJids: [],
        verifiedSenderJids: [],
        additionalAdminPhones: [],
      });
      expect(mod.isEnabled()).toBe(true);
    });
  });
  ```

  Note: The import path `../aprendizado-continuo.disabled.js` will NOT exist until Plan 01 creates it — this stub will fail on import, which is the expected Wave 0 state. Use `--passWithNoTests` flag to prevent CI failure before Plan 01.
  </action>
  <verify>
    <automated>npm --prefix apps/api test -- --reporter=verbose --testPathPattern=aprendizado-continuo.interface --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/api/src/modules/instances/__tests__/aprendizado-continuo.interface.test.ts`
    - File contains `import.*DisabledAprendizadoContinuoModule`
    - File contains `import.*ActiveAprendizadoContinuoModule`
    - File contains `describe('DisabledAprendizadoContinuoModule`
    - File contains 7 `it(` blocks
  </acceptance_criteria>
  <done>File exists with 7 failing test stubs importing from the Plan 01 target path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmation gate + knowledge audit stubs (APR-02, APR-04, APR-05)</name>
  <files>
    apps/api/src/modules/chatbot/__tests__/escalation-confirmation-gate.test.ts
    apps/api/src/modules/chatbot/__tests__/knowledge-audit.test.ts
  </files>
  <read_first>
    - apps/api/src/modules/chatbot/__tests__/chatbot-fallback.test.ts (existing chatbot test style)
    - apps/api/src/modules/chatbot/escalation.service.ts (lines 1–80 — EscalationService constructor and map declarations)
    - apps/api/src/modules/chatbot/knowledge.service.ts (lines 1–80 — save() signature)
  </read_first>
  <behavior>
    escalation-confirmation-gate.test.ts:
    - Test 1: Admin reply triggers confirmation echo message; `knowledgeService.save()` is NOT called immediately
    - Test 2: Admin follow-up "SIM" (normalized) triggers `knowledgeService.save()` and clears Redis confirmation key
    - Test 3: Admin follow-up "ok" does NOT trigger `knowledgeService.save()`
    - Test 4: Admin follow-up "claro" does NOT trigger `knowledgeService.save()`
    - Test 5: Redis key `confirmation:${instanceId}:${adminPhone}` has 600s TTL set after first admin reply

    knowledge-audit.test.ts:
    - Test 1: After `knowledgeService.save()` is called through the confirmation gate, the resulting row has `confirmedAt` as a non-null Date
    - Test 2: After save, the resulting row has `confirmedByJid` matching the admin's JID
  </behavior>
  <action>
  Create `escalation-confirmation-gate.test.ts`:

  ```typescript
  import { describe, it, expect, vi, beforeEach } from 'vitest';
  // Stub — will fail until Plan 02 refactors EscalationService.
  // Tests are written against the post-refactor interface.
  import { EscalationService } from '../escalation.service.js';

  describe('EscalationService — Confirmation Gate (APR-02, APR-04)', () => {
    it.todo('admin reply triggers confirmation echo; knowledgeService.save NOT called immediately');
    it.todo('admin follow-up "SIM" triggers knowledgeService.save and deletes Redis key');
    it.todo('admin follow-up "ok" does NOT trigger knowledgeService.save');
    it.todo('admin follow-up "claro" does NOT trigger knowledgeService.save');
    it.todo('Redis confirmation key has TTL=600 after first admin reply');
  });
  ```

  Create `knowledge-audit.test.ts`:

  ```typescript
  import { describe, it, expect } from 'vitest';
  // Stub — will fail until Plan 03 adds confirmedAt/confirmedByJid to schema and service.
  import { KnowledgeService } from '../knowledge.service.js';

  describe('KnowledgeService — Audit Metadata (APR-05)', () => {
    it.todo('saved knowledge entry has confirmedAt populated as non-null Date');
    it.todo('saved knowledge entry has confirmedByJid matching admin JID');
  });
  ```

  Use `it.todo()` for all tests so the runner sees them as pending (not failed), but the file still provides the scaffold for Plan 02 and 03 to replace with real implementations.
  </action>
  <verify>
    <automated>npm --prefix apps/api test -- --reporter=verbose --testPathPattern="escalation-confirmation-gate|knowledge-audit" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/api/src/modules/chatbot/__tests__/escalation-confirmation-gate.test.ts`
    - File exists at `apps/api/src/modules/chatbot/__tests__/knowledge-audit.test.ts`
    - `escalation-confirmation-gate.test.ts` contains 5 `it.todo(` entries
    - `knowledge-audit.test.ts` contains 2 `it.todo(` entries
    - Both files import from their respective service paths
  </acceptance_criteria>
  <done>Two stub files exist with todo-marked tests. Runner exits 0 with --passWithNoTests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Follow-up service + session metrics urgency stubs (FOL-01, FOL-02, URG-01)</name>
  <files>
    apps/api/src/modules/instances/__tests__/follow-up.service.test.ts
    apps/api/src/modules/instances/__tests__/session-metrics-collector.test.ts
  </files>
  <read_first>
    - apps/api/src/modules/instances/__tests__/session-metrics-collector.test.ts (existing file — read current content before modifying)
    - apps/api/src/modules/instances/session-metrics-collector.ts (lines 60–129 — onUrgencyDetected already present)
    - apps/api/src/queues/queue-names.ts (current queue names — FOLLOW_UP not yet present)
    - apps/api/src/queues/session-timeout-queue.ts (BullMQ queue pattern to replicate)
  </read_first>
  <behavior>
    follow-up.service.test.ts:
    - Test 1: `scheduleFollowUp()` with `lastContactAt = now - 23h` creates a BullMQ job (within 24h window)
    - Test 2: `scheduleFollowUp()` with `lastContactAt = now - 25h` returns `{ blocked: true, reason: 'outside_24h_window' }` and does NOT create a BullMQ job
    - Test 3: `scheduleFollowUp()` with target time between 21:00–08:00 São Paulo returns `{ blocked: true, reason: 'outside_business_hours' }` 
    - Test 4: Blocked follow-up is persisted to `ScheduledFollowUp` table with `status = 'blocked'` and `blockedReason` set
    - Test 5: Admin force-override (explicit flag) logs the override and creates the job despite the block

    session-metrics-collector.test.ts (EXTEND, do not replace):
    - Add one new `it.todo()` test: "session.urgency_detected event writes urgencyScore to ConversationSession DB row"
  </behavior>
  <action>
  Create `follow-up.service.test.ts`:

  ```typescript
  import { describe, it, expect } from 'vitest';
  // Stub — will fail until Plan 04 creates FollowUpService.
  // import { FollowUpService } from '../follow-up.service.js';

  describe('FollowUpService — 24h Window + Business Hours (FOL-01, FOL-02)', () => {
    it.todo('scheduleFollowUp within 24h window creates BullMQ job');
    it.todo('scheduleFollowUp outside 24h window returns blocked:true and no BullMQ job');
    it.todo('scheduleFollowUp outside business hours (21:00-08:00 Sao Paulo) returns blocked:true');
    it.todo('blocked follow-up persisted to ScheduledFollowUp table with status=blocked');
    it.todo('force-override flag logs override and creates BullMQ job despite 24h block');
  });
  ```

  For `session-metrics-collector.test.ts`: READ the current file content first, then APPEND a new `describe` block at the end:

  ```typescript
  describe('SessionMetricsCollector — urgency_detected (URG-01)', () => {
    it.todo('session.urgency_detected event writes urgencyScore to ConversationSession DB row via $executeRawUnsafe');
  });
  ```

  Do NOT modify existing tests in `session-metrics-collector.test.ts` — only append.
  </action>
  <verify>
    <automated>npm --prefix apps/api test -- --reporter=verbose --testPathPattern="follow-up.service|session-metrics-collector" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/api/src/modules/instances/__tests__/follow-up.service.test.ts`
    - `follow-up.service.test.ts` contains 5 `it.todo(` entries
    - `apps/api/src/modules/instances/__tests__/session-metrics-collector.test.ts` contains `urgency_detected` in a new describe block
    - `session-metrics-collector.test.ts` does NOT have any existing test blocks removed
    - Both commands exit 0 with `--passWithNoTests`
  </acceptance_criteria>
  <done>Follow-up stub file created. Session-metrics-collector test extended with urgency todo. No existing tests broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → production module import | Test stubs import from paths that don't exist yet — this is intentional and must not be treated as a build error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-00-01 | Tampering | Test stub files | accept | Wave 0 stubs are intentionally failing — no production code paths are affected; runner uses `--passWithNoTests` |
</threat_model>

<verification>
All three tasks complete when:
- `npm --prefix apps/api test -- --passWithNoTests` exits 0
- 5 test files exist at their declared paths
- No existing passing tests in `session-metrics-collector.test.ts` are broken
</verification>

<success_criteria>
- 5 test files exist at declared paths
- All stub tests are marked `it.todo()` or import from Plan 01 target paths (expected to fail on import until Plan 01 runs)
- Full test suite still exits 0 with `--passWithNoTests`
- `grep -r "it.todo" apps/api/src/modules/instances/__tests__/follow-up.service.test.ts | wc -l` returns 5
</success_criteria>

<output>
After completion, create `.planning/phases/08-continuous-learning-polish-advanced-features/08-00-SUMMARY.md`
</output>
